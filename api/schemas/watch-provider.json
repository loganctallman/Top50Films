{
  "$id": "watch-provider",
  "type": "object",
  "required": ["provider_id", "provider_name", "streaming_type"],
  "properties": {
    "provider_id":    { "type": "integer" },
    "provider_name":  { "type": "string", "minLength": 1 },
    "logo_path":      { "type": ["string", "null"] },
    "streaming_type": { "type": "string", "enum": ["flatrate", "free", "ads"] },
    "watch_link":     { "type": ["string", "null"] }
  },
  "additionalProperties": false
}
